name: Build and Push ML Job

on:
  push:
    branches: [main]
    paths:
      - 'recommendation-api/**'
      - '.github/workflows/build-push-recommendation-api.yml'

permissions:
  contents: write

jobs:
  build-and-deploy-training:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Set version tag
        id: version
        run: |
          # Get the current version from the manifest, increment patch version
          CURRENT_TAG=$(grep "image: caiosgrossi/playlist-recommender-system:training-service-" k8s/deployment-training.yaml | sed 's/.*training-service-//' | sed 's/".*//')
          
          # Check if current tag follows semantic versioning (vX.Y.Z)
          if [[ "$CURRENT_TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            # Extract version without 'v' prefix
            CURRENT_VERSION="${CURRENT_TAG#v}"
            IFS='.' read -r MAJOR MINOR PATCH <<< "$CURRENT_VERSION"
            PATCH=$((PATCH + 1))
            VERSION="${MAJOR}.${MINOR}.${PATCH}"
          else
            # Old format or first time, start with v1.0.0
            VERSION="1.0.0"
          fi
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "Building version: training-service-v$VERSION"

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build Docker image
        run: |
          docker build -t caiosgrossi/playlist-recommender-system:training-service-v${{ steps.version.outputs.VERSION }} \
                       -t caiosgrossi/playlist-recommender-system:training-service-latest \
                       training-service/

      - name: Push Docker image
        run: |
          docker push caiosgrossi/playlist-recommender-system:training-service-v${{ steps.version.outputs.VERSION }}
          docker push caiosgrossi/playlist-recommender-system:training-service-latest

      - name: Update Kubernetes manifest
        run: |
          sed -i "s|image: caiosgrossi/playlist-recommender-system:training-service-.*|image: caiosgrossi/playlist-recommender-system:training-service-v${{ steps.version.outputs.VERSION }}|g" k8s/deployment-training.yaml

      - name: Commit and push manifest change
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          git add k8s/deployment-training.yaml
          
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update ML training job to version v${{ steps.version.outputs.VERSION }}"
            git pull --rebase origin main
            git push
          fi

      - name: Workflow summary
        run: |
          echo "ML Training Job Build Complete"
          echo "Image: caiosgrossi/playlist-recommender-system:training-service-v${{ steps.version.outputs.VERSION }}"
          echo "Manifest: k8s/deployment-training.yaml updated"
          echo "ArgoCD will automatically deploy this version"
